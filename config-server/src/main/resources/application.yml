spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MatDev/MS-Gestion-Pedidos.git
          search-paths: config-repo
          default-label: user-service
  application:
    name: config-server
    

server:
  port: 8889


